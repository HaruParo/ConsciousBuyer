# Data Processing
pandas>=2.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# LLM Integration (Optional - only needed for use_llm_extraction or use_llm_explanations)
anthropic>=0.18.0  # LLM features with Opik tracing
# Requires ANTHROPIC_API_KEY environment variable

# Observability & Evaluation
opik>=0.1.0

# Web Framework (FastAPI for backend API)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
mangum>=0.17.0  # Serverless adapter for Vercel/AWS Lambda

# Configuration
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-cov>=4.0.0
