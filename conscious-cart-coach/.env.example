# =============================================================================
# Conscious Cart Coach - Environment Configuration
# =============================================================================
# Copy this file to .env and configure for your environment

# -----------------------------------------------------------------------------
# DEPLOYMENT ENVIRONMENT (Required)
# -----------------------------------------------------------------------------
# cloud = Use Anthropic Claude (for Vercel deployment)
# local = Use Ollama (for local development)
DEPLOYMENT_ENV=local

# -----------------------------------------------------------------------------
# CLOUD: Anthropic Configuration (Required when DEPLOYMENT_ENV=cloud)
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=your_anthropic_key_here
ANTHROPIC_MODEL=claude-3-haiku-20240307

# -----------------------------------------------------------------------------
# LOCAL: Ollama Configuration (Required when DEPLOYMENT_ENV=local)
# -----------------------------------------------------------------------------
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=mistral

# -----------------------------------------------------------------------------
# Opik Observability (Optional - for LLM tracing in cloud)
# -----------------------------------------------------------------------------
# OPIK_API_KEY=your_opik_key_here
# OPIK_WORKSPACE=your_workspace
# OPIK_PROJECT_NAME=consciousbuyer
